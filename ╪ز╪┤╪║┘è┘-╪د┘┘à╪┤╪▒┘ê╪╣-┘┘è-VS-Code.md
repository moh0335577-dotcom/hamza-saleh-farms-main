# تشغيل تطبيق مزارع حمزة صالح في VS Code

## المتطلبات

ثبّت Node.js بإصدار 18 أو أحدث، ثم ثبّت VS Code وامتداد Expo Tools اختياريًا.

## الخطوات

1. فك ضغط ملف المشروع وافتح المجلد في VS Code.
2. افتح Terminal داخل مجلد المشروع.
3. نفّذ الأمر التالي لتثبيت الحزم:

```bash
pnpm install
```

إذا لم يكن pnpm مثبتًا، نفّذ:

```bash
npm install -g pnpm
```

4. شغّل نسخة الويب للمعاينة:

```bash
pnpm dev:metro
```

5. لتشغيل التطبيق على الهاتف باستخدام Expo Go، نفّذ:

```bash
pnpm start
```

ثم امسح رمز QR الظاهر من خلال Expo Go. ولتشغيل Android من المحاكي استخدم `pnpm android`، ولتشغيل iPhone على macOS مع Xcode استخدم `pnpm ios`.

## محتوى التطبيق

يحتوي التطبيق على ثلاث شاشات: الرئيسية، معرض الصور، والتواصل معنا. أزرار الاتصال وواتساب والموقع تعمل عبر تطبيقات الهاتف المثبتة على الجهاز. رابط الخريطة وأرقام الهاتف موجودة في الملف `lib/farm-data.ts` ويمكن تعديلها عند الحاجة.

## فحص الكود

للتأكد من سلامة TypeScript نفّذ:

```bash
pnpm check
```

## ملاحظة

يجب تشغيل تطبيقات Android وiPhone الأصلية من خلال Expo Go أو محاكي مناسب. تشغيل الويب عبر VS Code مناسب للمعاينة، لكن بعض روابط الهاتف وواتساب تعمل بشكل أفضل على جهاز جوال فعلي.
